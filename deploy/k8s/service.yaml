apiVersion: v1
kind: Service
metadata:
  name: prism
  namespace: prism
spec:
  type: ClusterIP
  selector:
    app: prism
  ports:
    - name: grpc
      port: 8080
      targetPort: grpc
    - name: health
      port: 8081
      targetPort: health
